diff --git a/src/source/UI/NewUI/Character/NewUICharacterInfoWindow.cpp b/src/source/UI/NewUI/Character/NewUICharacterInfoWindow.cpp
index 25d6807..5ea66f2 100644
--- a/src/source/UI/NewUI/Character/NewUICharacterInfoWindow.cpp
+++ b/src/source/UI/NewUI/Character/NewUICharacterInfoWindow.cpp
@@ -373,7 +373,15 @@ void SEASON3B::CNewUICharacterInfoWindow::RenderTableTexts()
         g_pRenderText->SetFont(g_hFontBold);
         g_pRenderText->SetTextColor(255, 138, 0, 255);
         g_pRenderText->SetBgColor(0, 0, 0, 0);
-        g_pRenderText->RenderText(m_Pos.x + 110, m_Pos.y + 58, strLevelUpPoint);
+        // El sitio de los puntos estaba fijo en +110, y el texto de la izquierda
+        // ("Nivel: 400 | Resets: 18") pasa de esos 92 pixeles en cuanto el nivel tiene tres
+        // cifras y hay resets, asi que los dos textos se pisaban. Pasa igual en ingles: no es
+        // cosa de la traduccion. Ahora se mide lo que ocupa el texto de la izquierda y los
+        // puntos se ponen justo detras, con 8 pixeles de aire. Reportado por Stefano el
+        // 17-09-2026 con captura: «Resets: 18|Punto: 112».
+        const SIZE sizeLevel = g_pRenderText->MeasureText(strLevel, (int)wcslen(strLevel));
+        const int iPointX = m_Pos.x + 18 + sizeLevel.cx + 8;
+        g_pRenderText->RenderText(iPointX, m_Pos.y + 58, strLevelUpPoint);
     }
 
     g_pRenderText->SetFont(g_hFont);
